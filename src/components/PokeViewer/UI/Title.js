import styled from 'styled-components';
import { colorPokeViewerTitleBorder } from '../../../tokens';

const Title = styled.h1`
  color: ${colorPokeViewerTitleBorder};
  text-transform: uppercase;
  font-size: 18px;
`;

export default Title;
